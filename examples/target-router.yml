# Historical copy-and-paste workflows are intentionally retired in 0.2.
# Generate a complete, policy-specific workflow with both references pinned:
#
# github-agent-router provision OWNER/REPO --policy POLICY_JSON \
#   --router-ref REVIEWED_FULL_COMMIT_SHA --dry-run
#
# Then rerun without --dry-run after credentials and source access are verified.
# The provisioner opens/reuses a chatgpt/ PR; it does not write the default branch.
# Public targets require an approved public-safe distribution before activation.
# See docs/OPERATIONS.md. This comment-only file is not an installable workflow.
